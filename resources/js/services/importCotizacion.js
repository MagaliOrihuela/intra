

export default class ImportCot {

   getSubtotalCot(partidasCot){
      // var partidas = 0;
      // partidasCot.forEach(function(part,desc1,desc2,desc3) {

      //    // switch (part.unit) { // aqui asignamos el metro total dependiendo de la unidad
      //    //    case 'M2': 
      //    //        $mts = round((part.width * part.high),3);
      //    //        if($mts < 1) { $mtsr = 1; } else { $mtsr = $mts; }
      //    //        $subtotal_part = ( $mtsr * part.price ) * part.quantity; 
      //    //    break;
      //    //    case 'ML': 
      //    //        if(part.products_id == 6) {
      //    //            $mts = round(part.width,3);
      //    //        } elseif( part.products_id == 3 ) { 
      //    //             // si es lienzo verificamos si esta invertido 
      //    //             if( part.inverted == 1 ) { $mts = round(part.width,3); } else { $mts = round(part.high,3); }
      //    //          } else {
      //    //             if( part.high == 0 ) { $mts = round(part.width,3);  } else { $mts = round(part.high,3); } 
      //    //          }
      //    //        $subtotal_part = ( $mts * round(part.price,4))  * part.quantity ;
      //    //    break;
      //    //    case 'Pieza': 
      //    //        $mts = 0; 
      //    //        $subtotal_part = part.price * part.quantity ;
      //    //    break;
      //    //    case 'Paq': 
      //    //        $mts = 0; 
      //    //        $subtotal_part = part.price * part.quantity ;
      //    //    break;
      //    // }

      //    $quote_amount = $quote_amount + $subtotal_part; // obtenemos el importe

      //    // Obtenemos descuentos

      //    $dc1 = $subtotal_part - ( $subtotal_part * ( $cot->desc1 / 100 ) );
      //    $dc2 = $dc1 - ( $dc1 * ( $cot->desc2 / 100 ) );
      //    $subtotal_quote = $dc2 - ( $dc2 * ( $cot->desc3 / 100 ) );

      //    $discount_quote = $discount_quote + ($subtotal_part - $subtotal_quote);
         

      // });

      // $cot->total = '$ '.round( ( ( $quote_amount - $discount_quote ) * 1.16 ), 4 );
     return 0
   }

}